# list stored LISS Data Archive credentials

returns a data frame of usernames with stored credentials.

## Usage

``` r
liss_list_credentials()
```

## Value

a data frame with columns `service` and `username`.

## Examples

``` r
if (FALSE) { # \dontrun{
liss_list_credentials()
} # }
```
