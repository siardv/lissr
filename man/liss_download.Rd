% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/liss_download.R
\name{liss_download}
\alias{liss_download}
\title{download files from the LISS Data Archive}
\usage{
liss_download(
  .hosted = NULL,
  .dir = "liss",
  .modules = NULL,
  .waves = NULL,
  .unzip = TRUE
)
}
\arguments{
\item{.hosted}{a tibble with at least columns \code{module}, \code{wave}, \code{file},
and \code{path}, as returned by \code{\link[=liss_select]{liss_select()}} or \code{get_hosted_files()}.
if \code{NULL}, calls \code{get_hosted_files()} internally.}

\item{.dir}{character. local directory to save files to. created
automatically if it does not exist.}

\item{.modules}{optional character or integer vector to filter by module.}

\item{.waves}{optional integer vector to filter by wave number.}

\item{.unzip}{logical. if \code{TRUE} (the default), ZIP files are extracted
and the archive is removed.}
}
\value{
a tibble of download results with columns \code{file} and \code{status}
(invisibly).
}
\description{
downloads the files described in a selection tibble (typically produced
by \code{\link[=liss_select]{liss_select()}}) to a local directory. requires an active session
established by \code{\link[=liss_login]{liss_login()}}.
}
\examples{
\dontrun{
liss_login()
selection <- liss_select()
liss_download(selection)
}
}
