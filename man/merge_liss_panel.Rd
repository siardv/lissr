% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/liss_merge_engine.R
\name{merge_liss_panel}
\alias{merge_liss_panel}
\title{merge multiple LISS modules into a single panel dataset}
\usage{
merge_liss_panel(
  results,
  join_by = c("nomem_encr", "wave_year"),
  shared_cols = c("nohouse_encr"),
  join_type = c("full", "inner", "left"),
  write_to = NULL
)
}
\arguments{
\item{results}{either the named list returned by \code{\link[=merge_liss_modules]{merge_liss_modules()}}
(where each element has a \verb{$data} tibble), or a named list of data frames
directly. names should be module codes (e.g. \code{"ch"}, \code{"cv"}).}

\item{join_by}{character vector of columns to join on. defaults to
\code{c("nomem_encr", "wave_year")}.}

\item{shared_cols}{character vector of additional columns to keep
unprefixed, coalesced across modules in list order (first non-NA wins).
defaults to \code{c("nohouse_encr")}.}

\item{join_type}{character. type of join: \code{"full"} (default), \code{"inner"},
or \code{"left"} (keeps all rows from the first module).}

\item{write_to}{optional file path. if provided, the merged panel is written
as SAV (SPSS format, preserving labels). should end in \code{.sav}.}
}
\value{
a tibble with all modules joined side by side.
}
\description{
takes the output of \code{\link[=merge_liss_modules]{merge_liss_modules()}} (or a list of per-module
data frames) and joins them into one wide dataset keyed by respondent
and wave year. module-specific columns are prefixed with the module
code (e.g. \code{ch_s004}, \code{cv_s004}) to avoid name collisions. every module
must be one row per join key; the joins are performed with
\code{relationship = "one-to-one"} and abort otherwise.
}
\examples{
\dontrun{
results <- merge_liss_modules(recipe_paths, data_dir = "~/Downloads/liss")
panel <- merge_liss_panel(results)
panel <- merge_liss_panel(results, write_to = "./output/liss_panel.sav")
}
}
