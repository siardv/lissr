% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/liss_clean_income.R
\name{liss_clean_income}
\alias{liss_clean_income}
\title{Detect and correct implausible household-income values}
\usage{
liss_clean_income(
  data,
  background = NULL,
  ruleset = NULL,
  mode = c("correct", "flag", "na_only"),
  income_cap = NULL,
  min_income = NULL,
  disable = NULL,
  enable_only = NULL,
  params = NULL,
  variables = NULL,
  output_dir = NULL,
  verbose = TRUE
)
}
\arguments{
\item{data}{a data frame of merged LISS income data, or a
\code{\link[=merge_liss_module]{merge_liss_module()}} result (its \code{$data} element is used).}

\item{background}{optional background/demographics frame attached by
rule P01 before cleaning (joined on the person id and, when the
background is wave-stamped, the aligned annual wave index).}

\item{ruleset}{a ruleset object from \code{\link[=liss_cleaning_ruleset]{liss_cleaning_ruleset()}}, a
path to a ruleset YAML file, or \code{NULL} for the packaged default.}

\item{mode}{\code{"correct"} (apply corrections), \code{"flag"} (dry run,
propose only), or \code{"na_only"} (void detected cells without
imputation).}

\item{income_cap, min_income}{optional overrides for the global
plausibility constraints.}

\item{disable, enable_only}{optional character vectors of rule ids to
switch off, or to run exclusively.}

\item{params}{optional named list of per-rule parameter overrides,
e.g. \code{list(D06 = list(volatility_min = 0.7))}.}

\item{variables}{optional named list overriding entries of the
ruleset's variable mapping, e.g. \code{list(target = "ci00a339")}.}

\item{output_dir}{optional directory; when given, the report,
decision ledger, and JSONL log are written via
\code{\link[=liss_cleaning_report]{liss_cleaning_report()}}.}

\item{verbose}{print progress with cli.}
}
\value{
invisibly, a \code{liss_clean_result} list with elements \code{data},
\code{decisions} (the ledger), \code{log}, \code{summary}, \code{ruleset}, \code{variables},
and \code{mode}.
}
\description{
Applies a declarative income-cleaning ruleset to merged LISS data:
preparation (background attachment, sentinel guarding, sign
rectification, bracket-code expansion), global voiding of
unrecoverable values, household-level detection of scale errors,
bound violations, cap exceedances and robust statistical outliers,
constrained candidate-based correction, a hard plausibility cap, and
dataset-level flagging. Every decision is recorded in a ledger with
the responsible rule, the evidence, the admissible candidate set,
and a plain-language justification.
}
\details{
The returned data always carries the untouched input values in
\code{<target>_observed}. In \code{"correct"} mode the target column holds the
cleaned values and \code{<target>_clean_status} marks each modified cell
with its final action and rule. In \code{"flag"} mode the target column is
left untouched and the fully simulated result is returned in
\code{<target>_proposed} (with \code{<target>_proposed_status}), so the entire
procedure can be inspected as a dry run. In \code{"na_only"} mode detected
cells are voided instead of imputed. Dataset-level flags (rule D11)
are annotations in \code{<target>_dataset_flag} and never modify values.

Calling the function on already-cleaned data (recognizable by the
\code{<target>_observed} column) is an error, which prevents accidental
double cleaning.
}
\seealso{
\code{\link[=liss_cleaning_ruleset]{liss_cleaning_ruleset()}}, \code{\link[=liss_cleaning_report]{liss_cleaning_report()}},
\code{\link[=liss_equivalise_income]{liss_equivalise_income()}}
}
