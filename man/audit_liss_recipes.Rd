% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/audit_recipes.R
\name{audit_liss_recipes}
\alias{audit_liss_recipes}
\title{audit merge recipes for engine conformance}
\usage{
audit_liss_recipes(paths = NULL, quiet = FALSE)
}
\arguments{
\item{paths}{character vector of recipe file paths. default \code{NULL} audits
all bundled recipes.}

\item{quiet}{logical. \code{TRUE} suppresses the printed report.}
}
\value{
invisibly, a list with one entry per recipe (fields \code{module},
\code{schema_version}, \code{recipe_version}, \code{n_waves}, \code{covered_waves_match},
\code{noncanonical_patterns}, \code{nonconforming_rules}, \code{checks}) plus a
\code{totals} entry.
}
\description{
scans recipes (by default all ten bundled ones) and reports, per module,
whether every rule payload conforms to its action's specification, how the
declared validation checks classify (executable, documentary, skip), and
whether wave metadata is internally consistent. this turns the scattered
authoring-time warnings into one corpus-level conformance report, suitable
for CI gating.
}
\details{
for each recipe the audit reports: schema and recipe versions; wave count;
whether \code{meta$covered_waves} equals the \code{wave_index} ids; whether every
\code{file_pattern} is the canonical glob built from the wave id, in the
\verb{ch07a_*} style (documented pins are listed, not flagged); rules whose
payload carries keys their action does
not read (per the payload registry in \code{action_vocabulary.yml}); and the
validation-check classification counts.
}
\examples{
\dontrun{
audit <- audit_liss_recipes()
audit$totals
}
}
